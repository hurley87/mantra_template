import React from 'react';
import IndexLayout from './IndexLayout.jsx'

const DivIndex = ({divisions}) => (
  <IndexLayout questions={divisions} title='Division Exercises'/>
);

export default DivIndex;
